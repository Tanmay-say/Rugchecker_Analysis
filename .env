# Port for the server to listen on
PORT=3000

# Optional: JWT token for authenticated endpoints
# To get a token:
# 1. Sign the message "Sign-in to Rugcheck.xyz" with your Solana wallet
# 2. Send the signature and wallet address to /auth/login/solana
# 3. Put the returned token here
RUGCHECK_JWT_TOKEN=123
